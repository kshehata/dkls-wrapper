/**
 * This file contains a single JUnit/Kotlin test that asserts `true` to confirm the test
 * infrastructure is wired correctly and can run within the project.
 */
package app

import kotlin.test.Test
import kotlin.test.assertTrue

class SmokeTest {
    @Test
    fun testFrameworkRuns() {
        assertTrue(true)
    }
}
