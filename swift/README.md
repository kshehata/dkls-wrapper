# Swift Bindings

This folder contains the project file and extensions to be mixed in with the generated Swift bindings, it is not intended to be built directly. To build the Swift bindings, go back up to the root folder and run the build script: `./build-swift.sh`.

Once built you can refer to the `MobileTSS` folder as a package in Xcode.
